---
pt-BR:
  activerecord:
    models:
      user: Usuário
    attributes:
      account_share:
        user: Usuário
    errors:
      models:
        account_share:
          attributes:
            user:
              owner_self_share: já é o(a) dono(a) desta conta.
        user:
          attributes:
            base:
              email_not_valid: Email não é válido.
